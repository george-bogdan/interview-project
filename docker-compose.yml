version: '3.8'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./user-data/uploads:/app/user-data/uploads
    environment:
      - NODE_ENV=production